@model IEnumerable<Parcial2.Models.LABORATORIOS>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LabNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LabMail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LabDireccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LabNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LabMail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LabDireccion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LabId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LabId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LabId })
        </td>
    </tr>
}

</table>
